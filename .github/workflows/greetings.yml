name: Greetings

on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: "🐛 You’ve summoned your first issue! Thanks for reporting — our debugging hamsters are on it."
        pr-message: "🚀 First pull request detected! Initiating code review sequence… thanks for helping make this project better."

